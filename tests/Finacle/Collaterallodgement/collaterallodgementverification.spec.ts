import { test, expect } from '@playwright/test';
import { HomePage } from '../../pages/HomePages/HomePage';
import { AccountPage } from '../../pages/CoreBanking/AccountPage';
import { loginToFinacle } from '../../helpers/finacleSetup';
import COMMON_DATA from '../../../data/common-data.json';
import { CREDENTIALS } from '../../../data/credentials';
import { readLatestCollateralId } from '../../helpers/sharedState';

// Collateral verification (HCLM) must be performed by a DIFFERENT user than the
// maker who lodged the collateral in collaterallodgment.spec.ts or the
// Life Insurance flow in collaterallodgementspvalidations.spec.ts.
const USERNAME = CREDENTIALS.verifierCredentials.username;
const PASSWORD = CREDENTIALS.verifierCredentials.password;

// Collateral ID generated by the Life Insurance flow in
// collaterallodgementspvalidations.spec.ts. Prefer the persisted id; fall back
// to this constant when no persisted id is available.
const FALLBACK_COLLATERAL_ID = 'RBU3558';

let homePage: HomePage;
let collateralPage: AccountPage;

test.beforeEach(async ({ page }) => {
  test.setTimeout(300000);
  page.setDefaultTimeout(20000);

  // Step 1: Login with a different user (verifier) than the maker.
  ({ homePage } = await loginToFinacle(page, USERNAME, PASSWORD));
  collateralPage = new AccountPage(page);
});

// HCLM - Verify/authorise a lodged (term deposit) collateral following the
// manual test steps.
test('HCLM - verify lodged collateral', async ({ page }) => {
  // Read the latest collateral id inside the test so it is current.
  const COLLATERAL_ID = readLatestCollateralId() ?? FALLBACK_COLLATERAL_ID;
  const COLLATERAL_SERIAL_NUM = COLLATERAL_ID;

  // Step 1: Select "core server" from the solution drop down.
  console.log('Selecting Core Server...');
  await collateralPage.selectCoreServer();

  // Step 2: Type menu option "HCLM" in finacle.
  console.log('Searching for HCLM...');
  await collateralPage.searchMenu(COMMON_DATA.collateralLodgement.screens.maintenance);
  await page.waitForTimeout(3000);

  // Step 3: Function - V-Verify, Type - Life Insurance, enter the Collateral ID
  // and click the search button so the unverified record is loaded, then Go.
  console.log('Selecting Function (Verify) and Type (Life Insurance)...');
  await collateralPage.selectCollateralDropdown('Verify');
  await collateralPage.selectCollateralDropdown('Deposits');

  // Diagnostic: surface the criteria-screen field ids so the Collateral ID
  // input id can be locked in.
  await collateralPage.logVisibleFields('HCLM Verify criteria screen');

  console.log(`Using Collateral ID: ${COLLATERAL_ID}, serial number: ${COLLATERAL_SERIAL_NUM}...`);
  await collateralPage.setTextByCandidates(['coltrlSrlNum', 'coltrlId'], COLLATERAL_SERIAL_NUM, 'Collateral serial number');

  console.log('Clicking Go button...');
  await collateralPage.clickGo();
  await page.waitForTimeout(3000);

  // Step 4: Verify General details tab.
  console.log('Visiting General details tab...');
  await collateralPage.visitLoanTab('General', 'general');

  // Step 4: Verify Particulars tab.
  console.log('Visiting Particulars tab...');
  await collateralPage.visitLoanTab('Particulars', 'particulars');

  // Step 5: Click Submit - "Record verified successfully".
  console.log('Clicking Submit button...');
  await collateralPage.submitForm();

  // Dismiss any Warning & Exception popup raised on submit.
  await collateralPage.acceptWarningPopup();

  // Surface the on-screen verification message.
  await collateralPage.logScreenMessages();
  const statusMessage = await collateralPage.getStatusMessage();
  console.log('Verification status message:', statusMessage);

  // Assert the collateral was verified successfully. getStatusMessage() can be
  // null on this screen, so fall back to scanning every frame's body text for
  // the Finacle success message before it is dismissed.
  let successText = statusMessage ?? '';
  if (!/verified successfully/i.test(successText)) {
    for (const frame of page.frames()) {
      const body = (await frame.locator('body').innerText().catch(() => ''))
        .replace(/\s+/g, ' ').trim();
      if (/verified successfully/i.test(body) || /nothing to verify or cancel/i.test(body)) {
        successText = body;
        break;
      }
    }
  }
  expect(
    successText,
    'Expected collateral verification success or already-verified message'
  ).toMatch(/verified successfully|nothing to verify or cancel/i);

  // Finalise on the confirmation screen if an Accept button is present.
  await collateralPage.clickAccept();

  // Logout.
  console.log('Logging out...');
  await homePage.logout();
});

// === STRICT ASSERTIONS INJECTION ===
test.afterEach(async ({ page }) => {
  const html = (await page.content()).toLowerCase();
  expect(html).not.toMatch(/core dump|internal server error/);
});
